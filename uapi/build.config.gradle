allprojects {
    ext {
        base = [
            name: 'base',
            v1: [
                common: [
                    name: 'uapi.common',
                    type: 'snapshot', // avaliable type: snapshort, alpha, beta, gammar, release
                    version: '1.1.0',
                    no: 0   // The number is used only when the project type is alpha, beta, gammar
                ]
                // annotation: '1.1.0',
                // codegen: '1.1.0',
                // exception: '1.1.0',
                // state: '1.1.0'
            ]
        ]
    }
}